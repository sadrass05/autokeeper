# 自定义 App 图标 - 快速开始

## 📂 已为你准备好！

我已经为你创建了一套完整的自定义图标方案。现在你可以轻松添加自己的 PNG 图标了！

---

## 🚀 3步替换图标

### 第一步：准备你的图标

1. 准备你的图标图片（PNG格式，透明背景）
2. 推荐尺寸：**192x192 像素**
3. 你需要两个版本：
   - `ic_launcher.png` - 标准图标
   - `ic_launcher_round.png` - 圆形图标（可选，可用同一张）

### 第二步：放置图标文件

将你的图标放到指定位置：

```
custom_icons/
├── ic_launcher/
│   └── ic_launcher.png          ← 把你的标准图标放这里
└── ic_launcher_round/
    └── ic_launcher_round.png    ← 把你的圆形图标放这里
```

### 第三步：运行脚本（自动缩放）

在项目根目录打开终端，运行：

```bash
# 1. 进入 custom_icons 目录
cd custom_icons

# 2. 安装依赖（如果还没安装）
pip install -r requirements.txt

# 3. 运行替换脚本
python replace_icon.py
```

脚本会自动：
- 将你的图标缩放到所有需要的尺寸
- 复制到正确的文件夹位置

---

## 📦 或者手动替换

如果你不想用脚本，也可以手动复制：

| 尺寸 | 目标文件夹 |
|------|-----------|
| 48x48 | `app/src/main/res/mipmap-mdpi/` |
| 72x72 | `app/src/main/res/mipmap-hdpi/` |
| 96x96 | `app/src/main/res/mipmap-xhdpi/` |
| 144x144 | `app/src/main/res/mipmap-xxhdpi/` |
| 192x192 | `app/src/main/res/mipmap-xxxhdpi/` |

每个文件夹需要放两个文件：`ic_launcher.png` 和 `ic_launcher_round.png`

---

## 🔨 重新构建应用

替换完图标后，重新构建：

```bash
# 清理缓存
./gradlew clean

# 重新构建
./gradlew assembleDebug
```

然后重新安装到设备即可！

---

## 💡 提示

- 如果没有圆形图标，可以直接用同一张图片
- 建议使用透明背景
- 图标内容居中，四周留边距
